<?xml version="1.0" encoding="UTF-8"?>
<!--
  CDC publisher uber-jar для встраивания в apacheignite/ignite:2.16.0 контейнер.

  Сборка:    mvn package
  Артефакт:  target/stmnt-consistency-ignite-cdc-1.0-SNAPSHOT-uber.jar
             — содержит наши классы + shared hashers + jackson + kafka-clients.
             ignite-core НЕ shadeит (provided — он уже в libs/ ignite-образа).

  Использование: COPY uber-jar в libs/ ignite-контейнера + добавить в
  ignite-config-template.xml LifecycleBean со ссылкой
  ru.sbrf.stmnt.ignite.kafka.cdc.KafkaCdcPublisherBean.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>ru.sbrf.pprb.stmnt.services</groupId>
        <artifactId>stmnt-consistency</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>stmnt-consistency-ignite-cdc</artifactId>
    <packaging>jar</packaging>

    <properties>
        <!-- Загружается в JVM apacheignite/ignite:2.16.0 (Java 8). -->
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <maven.compiler.release>8</maven.compiler.release>
    </properties>

    <dependencies>
        <dependency>
            <groupId>ru.sbrf.pprb.stmnt.services</groupId>
            <artifactId>stmnt-consistency-hashers</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- PUBLIC Apache Ignite 2.16 (НЕ Sber-Nexus). provided — образ уже несёт. -->
        <dependency>
            <groupId>org.apache.ignite</groupId>
            <artifactId>ignite-core</artifactId>
            <version>${ignite.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.ignite</groupId>
            <artifactId>ignite-spring</artifactId>
            <version>${ignite.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Эти shadeим (отсутствуют в дефолтном ignite-образе) -->
        <!-- 3.4.1 — последняя версия kafka-clients с Java 8 support; ignite-образ на Java 8. -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-clients</artifactId>
            <version>3.4.1</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.16.1</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
            <version>2.16.1</version>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>2.0.13</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <shadedArtifactAttached>true</shadedArtifactAttached>
                            <shadedClassifierName>uber</shadedClassifierName>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <artifactSet>
                                <excludes>
                                    <exclude>org.apache.ignite:*</exclude>
                                    <exclude>org.slf4j:*</exclude>
                                </excludes>
                            </artifactSet>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
